import { RiLogoutBoxRLine } from "react-icons/ri";
import React from "react";

const LogoutIcon = React.memo(() => {
  return <RiLogoutBoxRLine className="h-5 w-5" />;
});

export default LogoutIcon;
